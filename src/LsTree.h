#ifndef LSTREE_H
#define LSTREE_H

#include <vector>
#include <string>

// Function to list the tree contents (filenames only)
std::vector<std::string> ls_tree(const std::string& tree_hash);

#endif // LSTREE_H
